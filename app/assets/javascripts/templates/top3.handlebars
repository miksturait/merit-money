<h2>{{view.label}}</h2>
{{#if view.content}}
<ul>
  {{#each user in view.content}}
  <li>
    <img {{bindAttr src="user.gravatar" title="user.name"}} />
  </li>
  {{/each}}
</ul>
{{else}}
<p>{{view.blankState}}</p>
{{/if}}