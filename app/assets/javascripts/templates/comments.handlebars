<div class="row">
    <h2>Selleo team appreciate…</h2>
    <div class="col-span-6 comments">
        <h3>me for:</h3>
        {{#if controllers.myComments}}
            <ul class="comments-list">
            {{#each controllers.myComments}}
                {{view App.CommentEntryView}}
            {{/each}}
            </ul>
        {{else}}
            <p>You have received no comments :(</p>
        {{/if}}
    </div>

    <div class="col-span-6 comments">
        <h3>my colleagues for:</h3>
        {{#if controllers.otherComments}}
            <ul class="comments-list">
                {{#each controllers.otherComments}}
                    {{view App.CommentEntryView}}
                {{/each}}
            </ul>
        {{else}}
            <p>Whoa! No comments!</p>
        {{/if}}
    </div>
</div>